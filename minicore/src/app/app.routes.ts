import { Routes } from '@angular/router';
import {
    ReporteEnvios
} from './pages/reporte-envios/reporte-envios';

export const routes: Routes = [
    {
        path: '',
        component: ReporteEnvios
    }
];